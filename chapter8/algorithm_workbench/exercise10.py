# Program to split the string to list.

def main():
    levels = "Beginner, Average, Advanced, Expert"
    print(levels)

    # Splitting the string
    levels_list = levels.split(', ')
    print(levels_list)

main()